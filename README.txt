This is https://github.com/rwoodley/FormulaToy3.

This does not depend on any server. Dependencies on 3LineMatrix were removed. It runs entirely in the browser.

The app is what should be sync'd with s3 using `aws sync`.

